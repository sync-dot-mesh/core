[package]
name = "sync-mesh-proto"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Sync.Mesh gRPC wire contract — sync.proto and its generated bindings. The single source of truth for the wire format; owned by neither the server nor any one client."

[dependencies]
tonic.workspace = true
prost.workspace = true

[build-dependencies]
tonic-build = "0.12"
