//! Sync.Mesh sync engine core.
//!
//! Placeholder entry point — real work starts with change detection
//! (the `notify` crate) and Blake3-based snapshotting. See the org
//! profile README for the full architecture:
//! https://github.com/sync-dot-mesh/.github

fn main() {
    println!("sync-mesh-core: scaffold only, nothing implemented yet.");
}

#[cfg(test)]
mod tests {
    #[test]
    fn sanity() {
        assert_eq!(2 + 2, 4);
    }
}
